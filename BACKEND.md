# FMoIP Backend

This app does not require a custom backend for the MVP. It relies on a public radio directory API to fetch station lists and stream URLs by country. If a backend is added later, it could provide:

- Aggregation and caching of radio station data.
- Analytics and anonymous usage metrics.
- Subscription validation and user profiles.

For now, no backend is implemented.
